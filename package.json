{
  "name": "sass-seed",
  "version": "0.0.1",
  "description": "High-performed and wealthy Sass seed project ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./build.js --watch --file=seed",
    "build": "node ./build.js --file=seed"
  },
  "author": "Idan Yalovich (yalovich.com)",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.30.0"
  },
  "dependencies": {}
}
